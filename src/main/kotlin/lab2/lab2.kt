package lab2

/**
 * 2 лаба
 * (Не факт что у вас будет такая)
 * 1) Создайте рабочую структуру, которая принимает список имен файлов из командной строки.
 * Она открывает каждый файл, за исключением последнего, для чтения, а последний для записи.
 * Эта рабочая структура обрабатывает каждый входящий файл, используя неопределенную политику,
 * а затем пишет результат в последний файл.
 * Наследуйте от этой рабочей структуры для создания двух разный приложений:
 * • Первое конвертирует все литеры каждого файла в верхний регистр.
 * • Второе ищет файлы, в которых содержатся слова из первого файла.
 */
fun main() {
    val fileProcessor = FindFileWithWordsFromFirstFile()
    fileProcessor.process("1a.txt", "1b.txt", "1c.txt", "result.txt")
}